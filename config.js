'use strict';

module.exports = {
  // Application
  app: { host: 'localhost', port: 80 },

  // Database to use
  db: { host: 'localhost', port:27017, dbName: 'berrypod' },

  // JWT secret key
  jwtKey: 'secretkey123'
};
